int printStr (char *ch);
int printInt (int n);
int readInt (int *eP);

int main()
{
    int n;
    int a[10];
    int m;
    int i;
    Print
    n = readInt(&n);
    for(i = 0;i < n; i = i + 1)
    {
        m = readInt(&m);
        a[i] = m;
    }
}